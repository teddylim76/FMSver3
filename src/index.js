import React from "react";
import ReactDOM from "react-dom/client";
import FaultManagementSystem from "./FaultManagementSystem";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <FaultManagementSystem />
  </React.StrictMode>
);
